# Goal

Create a Spring Boot REST controller named HealthController.

## Requirements:

- use @RestController
- base route: /api
- GET /health endpoint
- return simple string response
- use clean package structure
## Constrains:

- use layered architecture
- avoid unnecessary complexity
- use REST conventions